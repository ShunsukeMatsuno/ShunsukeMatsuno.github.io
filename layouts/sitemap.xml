{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{- $excludedKinds := slice "taxonomy" "term" }}
  {{- range where .Pages "Sitemap.Disable" "ne" true }}
    {{- if and .Permalink (not (in $excludedKinds .Kind)) }}
      <url>
        <loc>{{ .Permalink }}</loc>
        {{- if not .Lastmod.IsZero }}
          <lastmod>{{ .Lastmod.Format "2006-01-02T15:04:05-07:00" }}</lastmod>
        {{- end }}
        {{- if .IsTranslated }}
          {{- range .AllTranslations }}
            <xhtml:link
              rel="alternate"
              hreflang="{{ .Language.Locale }}"
              href="{{ .Permalink }}"
            />
          {{- end }}
        {{- end }}
      </url>
    {{- end }}
  {{- end }}

  {{- if .Site.IsDefault }}
    {{- range os.ReadDir "static/papers" }}
      {{- if and (not .IsDir) (strings.HasSuffix .Name ".pdf") }}
        <url>
          <loc>{{ printf "papers/%s" .Name | absURL }}</loc>
        </url>
      {{- end }}
    {{- end }}
    {{- if os.FileExists "static/cv/cv.pdf" }}
      <url>
        <loc>{{ "cv/cv.pdf" | absURL }}</loc>
      </url>
    {{- end }}
  {{- end }}
</urlset>
